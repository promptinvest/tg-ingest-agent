# Codex Session Instructions

The working agreement for this repo is `CLAUDE.md` — the shared source of
truth for ALL agents (Claude Code, Codex CLI, Copilot). Read it in full before
doing anything, then follow it exactly, including:

- The spec-update rule: every change updates BOTH `CARA.md` and `SOLUTION.md`
  in the same commit.
- The sibling-repo knowledge bases in `../Codex/` (PD-VPS host KB, fleet KB).
- Analyze architecture first; argue before acting when unsure.
